[tool.poetry]
name = "mod-notification-handler"
version = "1.0"
description = "processes notifications for moderators"
authors = ["Erin Aster <erin@arxiv.org>"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"
arxiv-base = {git = "https://github.com/arXiv/arxiv-base.git", rev = "2d29bd72e496ebe2c18ee9879ebeda43c3abd875"}
google-cloud-pubsub = "*"
requests = "*"

[tool.poetry.group.dev.dependencies]
pytest = "*"
coverage = "*"

[tool.coverage.run]
# Measure the app only. Without this, coverage also counts tests/, which are ~100% by
# construction and inflate the total — 93% reported vs 86% actually covered.
source = ["app"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
